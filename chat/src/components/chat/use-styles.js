import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => {
  return {
    panel: {
      display: "flex",
      justifyContent: "space-around",
      marginBottom: "20px",
    },
  };
});
